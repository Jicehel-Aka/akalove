# Projet ESP-IDF AKA-Love. Prérequis : ESP-IDF (v5.x) et le composant `gamebuino` dans components/
# (copie ou lien symbolique vers la bibliothèque des autres jeux AKA).
cmake_minimum_required(VERSION 3.16)
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(akalove)
